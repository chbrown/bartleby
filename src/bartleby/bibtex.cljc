(ns bartleby.bibtex
  (:refer-clojure :exclude [char])
  (:require [clojure.string :as string]
            [the.parsatron :refer :all]
            [clojure.set :as set]))

(defn parse-single
  [s]
  (println s "TODO"))

(defn parse
  [s]
  (println s "TODO"))
